#ifndef MODULES_DIRECTORY
    #define MODULES_DIRECTORY modules
#endif

class grad_randomizeMap {
        class common {
            file = MODULES_DIRECTORY\grad_randomizeMap\functions\common;
            class colorBuildings {};
            class replaceBuilding {};
        };
};
